/** Paths to real invoice files used by e2e tests (same text as backend fixtures). */
export const INVOICE_FIXTURES = {
  acme: "/invoices/acme-inv-001.txt",
  northwind: "/invoices/northwind-inv-12500.txt",
} as const;
