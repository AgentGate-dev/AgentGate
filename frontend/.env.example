# Point the demo dashboard at your hosted AgentGate API (Fly.io, Render, etc.).
NEXT_PUBLIC_AGENTGATE_API=https://agentgate-api.fly.dev

# Optional: turn trace_id into a clickable link (must contain {id})
# NEXT_PUBLIC_TRACE_URL_TEMPLATE=https://cloud.langfuse.com/project/xxx/traces/{id}
